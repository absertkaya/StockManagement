@inherits ItemScannerBase

@page "/scanner/{method}"
@using StockManagement.Pages.ReuseableComponents

    <div class="btn-group">
        <button class="btn btn-primary" @onclick="Submit">Geef in</button>
        <button class="btn btn-secondary" @ref="resetButton" id="resetButton">Reset</button>

        @if (Method == "in")
        {
            <button class="btn btn-warning" @onclick="Skip">Sla over</button>
        }

        <button class="btn btn-info float-right" @onclick="SwitchScanner">
            @if (!_quagga)
            {
                @("ZXing")
            }
            else
            {
                @("Quagga")
            }
        </button>
    </div>

@if (_quagga)
{
    <QuaggaScanner @ref="_scanner"/>
}
else
{
    <Scanner Kind="Serienummer" @ref="_scanner" />
}


@code {

}
